name: Validate Git Hygiene
on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - main

jobs:
  git-hygiene:
    uses: saaslabsco/ci-workflows/.github/workflows/git-hygiene-reusable.yml@master
    name: Validate Git Hygiene
